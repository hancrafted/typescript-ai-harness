# Build artifacts (this block is what the tool's generic template emits).
node_modules
coverage
dist

# Generated Core bundle asset — a Tool-owned byte mirror of .archgate/,
# regenerated by `npm run capture` (ADR-0010 §1).
assets

# archgate's @generated rule types: committed + seeded (ADR-0005 v5) but written
# in archgate's own style, so excluded from prettier — a reformat would drift from
# what `archgate check` regenerates. The archgate Integration appends this line.
.archgate/rules.d.ts

# Repo-specific additions: vendored skills, prose docs, and agent config
# predate this harness and are not the tool's own source. The deferred
# adaptation skill (ADR-0003) will generalize per-target ignores later.
.agents
.claude
docs
*.md
skills-lock.json
